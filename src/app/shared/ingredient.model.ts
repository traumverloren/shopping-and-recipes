export class Ingredient {
  // will give use these properties specified
  // and automatically assign the values we receive to these properties
  constructor(public name: string, public amount: number) {}
}
